题目简述：

> 给定一个由 4 位数字组成的数组，返回可以设置的符合 24 小时制的最大时间。
>
> 24 小时格式为 `"HH:MM"` ，其中 `HH` 在 `00` 到 `23` 之间，`MM` 在 `00` 到 `59` 之间。最小的 24 小时制时间是 `00:00` ，而最大的是 `23:59` 。从 00:00 （午夜）开始算起，过得越久，时间越大。
>
> 以长度为 5 的字符串，按 `"HH:MM"` 格式返回答案。如果不能确定有效时间，则返回空字符串。

题目链接：[949. 给定数字能组成的最大时间](https://leetcode.cn/problems/largest-time-for-given-digits/)

# 思路

回溯算法遍历所有可能的组合即可，因为本问题的计算量非常小。

最初我考虑模拟的方式，写了一会规则后，最后还是直接暴力回溯了。

# 代码

由于所有可能的组合数量有明确的上限且该上限极小，因此算法的时间复杂度为 $O(1)$，空间复杂度为 $O(1)$。

```java
class Solution {

    private int[] maxTime;

    public String largestTimeFromDigits(int[] arr) {
        maxTime = null;
        dfs(arr, new boolean[4], new int[4], 0);
        if (maxTime == null) return "";
        return "" + maxTime[0] + maxTime[1] + ":" + maxTime[2] + maxTime[3];
    }

    private void dfs(int[] arr, boolean[] used, int[] time, int i) {
        if (i == 4) {
            if (isVaildTime(time) && isBiggerTime(time, maxTime)) maxTime = Arrays.copyOf(time, 4);
            return; 
        }

        for (int j = 0; j < 4; j++) {
            if (!used[j]) {
                used[j] = true;
                time[i++] = arr[j];
                dfs(arr, used, time, i--);
                used[j] = false;
            }
        }
    }

    private boolean isVaildTime(int[] time) {
        int hour = time[0] * 10 + time[1];
        if (hour > 23) return false;
        int minute = time[2] * 10 + time[3];
        return minute < 60;
    }

    private boolean isBiggerTime(int[] a, int[] b) {
        if (b == null) return true;
        if (a == null) return false;
        int h1 = a[0] * 10 + a[1];
        int h2 = b[0] * 10 + b[1];
        if (h1 > h2) return true;
        if (h1 < h2) return false;
        int m1 = a[2] * 10 + a[3];
        int m2 = b[2] * 10 + b[3];
        return m1 > m2;
    }
}
```

