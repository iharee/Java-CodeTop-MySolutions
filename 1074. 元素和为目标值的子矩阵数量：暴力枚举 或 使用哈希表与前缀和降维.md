题目简述：

> 给出矩阵 `matrix` 和目标值 `target`，返回元素总和等于目标值的非空子矩阵的数量。
>
> 子矩阵 `x1, y1, x2, y2` 是满足 `x1 <= x <= x2` 且 `y1 <= y <= y2` 的所有单元 `matrix[x][y]` 的集合。
>
> 如果 `(x1, y1, x2, y2)` 和 `(x1', y1', x2', y2')` 两个子矩阵中部分坐标不同（如：`x1 != x1'`），那么这两个子矩阵也不同。

题目链接：[1074. 元素和为目标值的子矩阵数量](https://leetcode.cn/problems/number-of-submatrices-that-sum-to-target/)

# 四重循环暴力枚举

## 思路

对于子矩阵，不难知道我们至少可以通过四个值唯一确定一个子矩阵：

1. 左上角元素横坐标 $x$
2. 左上角元素纵坐标 $y$
3. 右下角元素横坐标 $\alpha$
4. 右下角元素纵坐标 $\beta$

这样，右上角元素的坐标为 $(\alpha,y)$，左下角元素的坐标为 $(x,\beta)$。

为避免重复计算，可以规定 $\alpha\geqslant x$、$\beta\geqslant y$。

---

现在我们有能力通过嵌套的四层循环遍历表达所有的子矩阵了。如何快速高效地计算子矩阵元素和呢？
$$
\begin{array}{@{}c@{\;}c@{\;}c@{}}
% beta
\begin{array}{c}
\vphantom{
\overbrace{
    \overbrace{
        \phantom{\boxed{\begin{matrix}\phantom{000}\end{matrix}}}
    }^{x}
    \quad
    \phantom{\boxed{\begin{matrix}\phantom{0000000}\end{matrix}}}
}^{\alpha}
}
\\[0pt]
\beta\;
\left\{
\begin{array}{c}
\vphantom{
\boxed{\begin{matrix}
\phantom{000}
\end{matrix}}
}
\\[3pt]
\vphantom{
\boxed{\begin{matrix}
\phantom{000}\\
\phantom{000}\\
\phantom{000}
\end{matrix}}
}
\end{array}
\right.
\\[3pt]
\vphantom{
\boxed{\begin{matrix}
\phantom{000}\\
\phantom{000}\\
\phantom{000}\\
\phantom{000}\\
\phantom{000}\\
\phantom{000}
\end{matrix}}
}
\end{array}
&
% y
\begin{array}{c}
\vphantom{
\overbrace{
    \overbrace{
        \phantom{\boxed{\begin{matrix}\phantom{000}\end{matrix}}}
    }^{x}
    \quad
    \phantom{\boxed{\begin{matrix}\phantom{0000000}\end{matrix}}}
}^{\alpha}
}
\\[0pt]
y\;
\left\{
\vphantom{
\boxed{\begin{matrix}
\phantom{000}
\end{matrix}}
}
\right.
\\[3pt]
\vphantom{
\boxed{\begin{matrix}
\phantom{000}\\
\phantom{000}\\
\phantom{000}
\end{matrix}}
}
\\[3pt]
\vphantom{
\boxed{\begin{matrix}
\phantom{000}\\
\phantom{000}\\
\phantom{000}\\
\phantom{000}\\
\phantom{000}\\
\phantom{000}
\end{matrix}}
}
\end{array}
&
% blocks
\begin{array}{c}

\overbrace{
    \overbrace{
        \phantom{
        \boxed{\begin{matrix}
        \phantom{000}
        \end{matrix}}
        }
    }^{x}
    \quad
    \phantom{
    \boxed{\begin{matrix}
    \phantom{0000000}
    \end{matrix}}
    }
}^{\alpha}
\quad
\phantom{
\boxed{\begin{matrix}
\phantom{00000000000}
\end{matrix}}
}
\\[0pt]
% I II III
\boxed{
\begin{matrix}
\phantom{0}\rlap{{\scriptstyle\mathrm{I}}}\phantom{00}
\end{matrix}
}
\quad
\boxed{
\begin{matrix}
\phantom{000}\rlap{{\scriptstyle\mathrm{II}}}\phantom{0000}
\end{matrix}
}
\quad
\boxed{
\begin{matrix}
\phantom{00000}\rlap{{\scriptstyle\mathrm{III}}}\phantom{000000}
\end{matrix}
}
\\[3pt]
% IV V VI
\boxed{
\begin{matrix}
\phantom{000}\\
\phantom{0}\rlap{{\scriptstyle\mathrm{IV}}}\phantom{00}\\
\phantom{000}
\end{matrix}
}
\quad
\boxed{
\begin{matrix}
\phantom{0000000}\\
\phantom{000}\rlap{{\scriptstyle\mathrm{V}}}\phantom{0000}\\
\phantom{0000000}
\end{matrix}
}
\quad
\boxed{
\begin{matrix}
\phantom{00000000000}\\
\phantom{00000}\rlap{{\scriptstyle\mathrm{VI}}}\phantom{000000}\\
\phantom{00000000000}
\end{matrix}
}
\\[3pt]
% VII VIII IX
\boxed{
\begin{matrix}
\phantom{000}\\
\phantom{000}\\
\phantom{0}\rlap{{\scriptstyle\mathrm{VII}}}\phantom{00}\\
\phantom{000}\\
\phantom{000}\\
\phantom{000}
\end{matrix}
}
\quad
\boxed{
\begin{matrix}
\phantom{0000000}\\
\phantom{0000000}\\
\phantom{000}\rlap{{\scriptstyle\mathrm{VIII}}}\phantom{0000}\\
\phantom{0000000}\\
\phantom{0000000}\\
\phantom{0000000}
\end{matrix}
}
\quad
\boxed{
\begin{matrix}
\phantom{00000000000}\\
\phantom{00000000000}\\
\phantom{00000}\rlap{{\scriptstyle\mathrm{IX}}}\phantom{000000}\\
\phantom{00000000000}\\
\phantom{00000000000}\\
\phantom{00000000000}
\end{matrix}
}
\end{array}
\end{array}
$$
可见，<u>只要我们有能力快速根据小矩阵右下角元素的坐标获得相应的前缀子矩阵的元素和 `sum[i][j]`，就能很快地根据上图的分解计算出目标子矩阵 Ⅴ 的元素和</u>。如此，计算指定小矩阵 Ⅴ 的内部元素和时间复杂度为 $O(1)$，有等式：
$$
\mathrm{res}_V=\mathrm{sum}_{V}-\mathrm{sum}_{IV}-\mathrm{sum}_{II}+\mathrm{sum}_{I}
$$
于是就不难编码了。该思路不能实现最优的时间复杂度，但简单、直观、暴力。

## 代码

算法的时间复杂度为 $O(m^2n^2)$，空间复杂度为 $O(mn)$。

```java
class Solution {
    public int numSubmatrixSumTarget(int[][] matrix, int target) {
        int m = matrix.length;
        int n = matrix[0].length;
        int res = 0;

        long[][] sum = new long[m + 1][n + 1];
        for (int i = 1; i <= m; i++) {
            long acc = 0;
            for (int j = 1; j <= n; j++) {
                acc += matrix[i - 1][j - 1];
                sum[i][j] = sum[i - 1][j] + acc;
            }
        }

        for (int x = 0; x <= m; x++) {
            for (int y = 0; y <= n; y++) {
                for (int a = x + 1; a <= m; a++) {
                    for (int b = y + 1; b <= n; b++) {
                        if (sum[a][b] - sum[a][y] - sum[x][b] + sum[x][y] == target) res++;
                    }
                }
            }
        }

        return res;
    }
}
```

# 利用哈希表降维建模

## 思路

如果考虑直接遍历所有的子矩阵，不可避免地涉及四重循环，时间复杂度不会低于 $O(m^2n^2)$。

有一个办法可以帮助我们降低时间复杂度：将二维问题转换为多个一维问题。具体做法是：固定子矩阵的上下边界（或左右边界），维护一个边界内列之和（或行之和）的前缀和数组，并将前缀和数组的值置于哈希表。这样，就可以类似两数之和问题，以 $O(1)$ 的时间复杂度为代价，快速判断固定上下行（或左右列）时，针对特定的右边界（或下边界），是否存在一个左边界（或上边界），使得四个边界构成的子矩阵内元素和满足 `target`。

由于列之和（或行之和）的前缀和数组是动态维护的，不需要每次都从零构建，因此总的时间复杂度为 $O(m^2n)$ 或 $O(mn^2)$——取决于固定两行还是两列。

## 代码

算法的时间复杂度为 $O(m^2n)$，空间复杂度为 $O(n)$。

```java
class Solution {
    public int numSubmatrixSumTarget(int[][] matrix, int target) {
        int m = matrix.length;
        int n = matrix[0].length;
        int res = 0;
        Map<Integer, Integer> map = new HashMap<>();

        for (int up = 0; up < m; up++) {
            int[] colSum = new int[n];
            for (int down = up; down < m; down++) {
                for (int j = 0; j < n; j++) colSum[j] += matrix[down][j];
                map.clear();
                map.put(0, 1);
                int prefixSum = 0;
                for (int j = 0; j < n; j++) {
                    prefixSum += colSum[j];
                    res += map.getOrDefault(prefixSum - target, 0);
                    map.put(prefixSum, map.getOrDefault(prefixSum, 0) + 1);
                }
            }
        }

        return res;
    }
}
```
