题目简述：

> 几张卡牌 **排成一行**，每张卡牌都有一个对应的点数。点数由整数数组 `cardPoints` 给出。
>
> 每次行动，你可以从行的开头或者末尾拿一张卡牌，最终你必须正好拿 `k` 张卡牌。
>
> 你的点数就是你拿到手中的所有卡牌的点数之和。
>
> 给你一个整数数组 `cardPoints` 和整数 `k`，请你返回可以获得的最大点数。

题目链接：[1423. 可获得的最大点数](https://leetcode.cn/problems/maximum-points-you-can-obtain-from-cards/)

# 思路

很显然，按照题目描述的规则，假设我们将前 $m$ 个元素纳入点数，那么显然地，我们也需要将后 $k-m$ 个元素纳入点数。因此，考虑滑动窗口，遍历所有可能的选取方式即可。

> 原本第一反应是 DFS + 记忆化搜索，稍微一想并不需要这样，因为选取元素只能连续地从首或尾开始共选取 $k$ 个元素，形成了一个固定窗口。

# 代码

算法的时间复杂度为 $O(k)$，空间复杂度为 $O(1)$。

```java
class Solution {
    public int maxScore(int[] cardPoints, int k) {
        int n = cardPoints.length;
        long sum = 0;
        for (int i = 0; i < k; i++) sum += cardPoints[i];
        long res = sum;

        for (int i = 0; i < k; i++) {
            sum = sum - cardPoints[k - i - 1] + cardPoints[n - i - 1];
            res = Math.max(res, sum);
        }

        return (int) res;
    }
}
```
