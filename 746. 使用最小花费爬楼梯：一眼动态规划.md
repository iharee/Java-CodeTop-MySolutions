题目简述：

> 给你一个整数数组 `cost` ，其中 `cost[i]` 是从楼梯第 `i` 个台阶向上爬需要支付的费用。
>
> 一旦你支付此费用，即可选择向上爬 **一个** 或者 **两个** 台阶。
>
> 你可以选择从下标为 `0` 或下标为 `1` 的台阶开始爬楼梯。
>
> 请你计算并返回达到楼梯顶部的 **最低** 花费，即最后一步之后的那个位置（下标 `cost.length`）。

题目链接：[746. 使用最小花费爬楼梯](https://leetcode.cn/problems/min-cost-climbing-stairs/)

# 思路

一眼动态规划，定义 $dp[i]$ 表示抵达第 $i$ 个台阶时需要付出的最低花费，则
$$
dp[i]=\min\{dp[i-2]+cost[i-2],\ dp[i-1]+cost[i-1]\}
$$

# 代码

算法的时间复杂度为 $O(n)$，考虑滚动数组优化空间开销，空间复杂度为 $O(1)$。

```java
class Solution {
    public int minCostClimbingStairs(int[] cost) {
        int dp1 = 0;
        int dp2 = 0;
        int n = cost.length;
        
        for (int i = 2; i <= n; i++) {
            int dp = Math.min(dp1 + cost[i - 1], dp2 + cost[i - 2]);
            dp2 = dp1;
            dp1 = dp;
        }
        
        return dp1;
    }
}
```

