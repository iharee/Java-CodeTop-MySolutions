题目简述：

> 给你一个可能含有 **重复元素** 的整数数组 `nums` ，请你随机输出给定的目标数字 `target` 的索引。你可以假设给定的数字一定存在于数组中。
>
> 实现 `Solution` 类：
>
> - `Solution(int[] nums)` 用数组 `nums` 初始化对象。
> - `int pick(int target)` 从 `nums` 中选出一个满足 `nums[i] == target` 的随机索引 `i` 。如果存在多个有效的索引，则每个索引的返回概率应当相等。

题目链接：[398. 随机数索引](https://leetcode.cn/problems/random-pick-index/)

# 两种抽样算法的比较

本问题我想实际希望考察的应该是蓄水池抽样算法（Reservoir Sampling），但我们暴力存储所有索引也能 AC。下文会分别给出两种抽样算法的实现，现在我们分别看看二者的性能差别。

|            | 哈希表存储所有索引 | Reservoir Sampling |
| ---------- | :----------------: | :----------------: |
| 构造       |        O(n)        |        O(1)        |
| `pick`     |      **O(1)**      |        O(n)        |
| 额外空间   |        O(n)        |      **O(1)**      |
| 适合的场景 | 能够存储全部的数据 |   流数据随机抽样   |

# 哈希表存储所有索引

这是最简单、暴力、直观的思路，直接使用哈希表存储所有「值 -> 索引」的映射，`pick` 操作的时间复杂度为 $O(1)$，空间复杂度为 $O(n)$。

```java
class Solution {

    private Random rand = new Random();
    private Map<Integer, List<Integer>> map;

    public Solution(int[] nums) {
        map = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            map.computeIfAbsent(nums[i], key -> new ArrayList<>()).add(i);
        }
    }
    
    public int pick(int target) {
        List<Integer> list = map.get(target);
        // if (list == null || list.isEmpty()) return -1;
        return list.get(rand.nextInt(list.size()));
    }
}

/**
 * Your Solution object will be instantiated and called as such:
 * Solution obj = new Solution(nums);
 * int param_1 = obj.pick(target);
 */
```

# 蓄水池抽样算法

蓄水池抽样算法能做到不额外使用空间，代价是 `pick` 的最坏复杂度。蓄水池抽样算法非常适合流数据随机抽样——数据流总长度未知且无法保存全部数据流，是解决该问题的一种经典算法。

该算法的做法很简单：遍历数据流，对第 $i$ 个数据（1-based），以 $\frac1{i}$ 的概率选取该数据。最终，完成一次遍历后，结果就是随机选取元素的索引。对于本问题，要求针特定值的索引进行随机选取，那么可以忽略非特定值数据，只关注特定值的索引即可。

证明是不困难的：
$$
\begin{aligned}
P(\text{pick}=i)
&= P(\text{第 }i\text{ 个元素被选中，并且之后未被替换}) \\
&= \frac{1}{i}\prod_{j=i+1}^{n}\left(1-\frac{1}{j}\right) \\
&= \frac{1}{i}\prod_{j=i+1}^{n}\frac{j-1}{j} \\
&= \frac{1}{i}
   \cdot
   \frac{i}{i+1}
   \cdot
   \frac{i+1}{i+2}
   \cdots
   \frac{n-1}{n} \\
&= \frac{1}{i}\cdot\frac{i}{n} \\
&= \frac{1}{n}
\end{aligned}
$$
这样，就能实现 `pick` 操作的时间复杂度为 $O(n)$ 而空间复杂度为 $O(1)$ 的蓄水池抽样算法。

```java
class Solution {

    private Random rand = new Random();
    private int[] nums;

    public Solution(int[] nums) {
        this.nums = nums;
    }
    
    public int pick(int target) {
        int res = -1;
        int count = 0;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] == target) {
                count++;
                int r = rand.nextInt(count);
                if (r == 0) res = i;
            }
        }
        return res;
    }
}

/**
 * Your Solution object will be instantiated and called as such:
 * Solution obj = new Solution(nums);
 * int param_1 = obj.pick(target);
 */
```

